{{extend 'layout.html'}}

{{=LOAD('default','start_callback',args=election.id,ajax=True,target='target')}}

<div id="demo">

<h2 style="color:red">{{=T('Test Ballot (not actual voting ballot)')}}</h2>

{{=demo}}

</div>

<script>
  var uls=[];
  jQuery('.model-ranking').attr('type','hidden').each(function(){var ul=jQuery(this).closest('ul'); if(uls.indexOf(ul)<0) uls.push(ul); });
  for(var k=0; k<uls.length; k++)
    uls[k].sortable({stop:function(){jQuery(this).find('input.model-ranking').each(function(i){jQuery(this).val(i+1);})} });
</script>
